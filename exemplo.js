class Pessoa {
    constructor(nome, idade, altura){
    this.nome = nome
    this.idade = idade
    this.altura = altura
    }
}

const user = new Pessoa("Ana Julia", 16, 1.6)
const user1 = new Pessoa ("Flavia", 16, 1.63)

console.log(user1);